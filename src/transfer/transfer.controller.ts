import { Controller } from '@nestjs/common';

@Controller('transfer')
export class TransferController {
  // criar trasferencia
    // não pode ser pra si mesmo,
    // ambas as contas precisa estar ativas,

  // função para pagar
  // leitor de função paga (mudar status)
  // estorno
    // so pode fazer até no maximo 10 minutos,
}
