import { Controller } from '@nestjs/common';

@Controller('account')
export class AccountController {

  // criar uma acc,
  // alterar uma conta,
  // ativar uma conta / desativar
  // adicionar valor (conta precisa ser ativa)
}
